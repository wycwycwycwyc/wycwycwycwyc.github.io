document.addEventListener('DOMContentLoaded', function() {
    hljs.configure({
    // 定义可能用到的语言
    languages: ['javascript', 'python', 'html', 'bash', 'cpp', 'java', 'json'],
    
    // 高亮所有代码块
}).highlightAll();
});